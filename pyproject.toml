[project]
name = "moviebot"
version = "1.0.0"
description = "MovieBot Telegram webhook"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "flask>=3.0.0",
]

[tool.vercel]
entrypoint = "api.webhook:app"